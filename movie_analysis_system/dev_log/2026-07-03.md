# 开发日志 — 2026-07-03

## v2.1 多源降级 + 像素级 UI 修复

### Part 1: 字段级多源数据获取（强制归因）

#### 剧情简介 — 三级降级
- **主源**：猫眼 H5 `ajax/movie?movieId={id}` → `data.movie.summary`
- **备源1**：豆瓣 Frodo API `frodo.douban.com/api/v2/movie/{douban_id}` → `summary`
- **备源2**：TMDB API `api.themoviedb.org/3/movie/{tmdb_id}` → `overview`
- **归因日志**：三级全部失败时输出 `[FALLBACK_REASON] summary: maoyan=404, douban=id_missing, tmdb=api_key_missing`
- **UI兜底**：保留区块，显示 📖 本片剧情简介暂未收录，[前往猫眼查看] 链接（#718096, 14px）

#### 观众热评 — 二级降级（已恢复）
- 重建被 v2.0 删除的短评区块完整 UI
- **主源**：猫眼 H5 `ajax/movie?movieId={id}` → `data.movie.comments`
- **备源**：豆瓣 Frodo API `frodo.douban.com/api/v2/movie/{douban_id}/comments`
- **归因日志**：`[FALLBACK_REASON] comments: maoyan=empty_array, douban=auth_required`
- **UI兜底**：保留标题"观众热评"，显示 💬 暂无短评，成为第一个评论者吧（#A0AEC0, 13px）

#### 豆瓣详情跳转 — 根因排查
- **归因日志**：`[FALLBACK_REASON] douban_link: id_missing AND search_failed (reason: no_exact_match)`
- **UI处理**：禁用 + `Qt.ForbiddenCursor` + tooltip"豆瓣ID未获取" + 无 clicked 信号绑定

### Part 2: UI 修复

#### 评分区单分支渲染
- 彻底消除"评分 9.6 + 暂无评分"视觉冲突
- `if rating > 0`: 显示"评分 X.X"（#FF6B35 36pt bold）+ "X,XXX 人评"
- `else`: 显示"评分待更新"（#A0AEC0 24pt），隐藏所有其他评分标签
- 彻底删除所有含"暂无"字样的 QLabel

#### 主演阵容 QLabel 确认
- `ActorCircleWidget` 使用纯 QLabel + QSS（`border-radius:24px; background:#98D8C8; color:white; 48×48`）
- `_build_cast` 中添加 `assert isinstance(avatar._label, QLabel)` 断言
- 支持 `;` 和 `／` 两种分隔符（修复此前 `／` 不分隔的 bug）
- 无 QPainter/paintEvent 残留

#### 左侧导航栏字体放大
- `font-size: 14px` → `font-size: 16pt`（约 21px）
- `font-weight: 500` → 选中态 `bold`
- 选中态：`background:#EBF8FF; color:#2B6CB0; border-left:4px solid #3182CE; padding-left:12px`
- 未选中态：`color:#4A5568; padding-left:16px`
- 行高固定 48px

### 新增配置
- `TMDB_API_KEY = ""`（位于 detail_page.py 顶部，留空 = 不启用 TMDB 降级）

### 验证
- 应用正常启动：120 条数据，23 条伪造数据被过滤
- 所有导入通过（detail_page.py / main_window.py / recommendation_page.py）
- 海报缓存命中，样式表加载成功

## 代码变更摘要

### 修改的文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `ui/detail_page.py` | 重写 | 多源降级逻辑 + 短评恢复 + 评分单分支 + 主演QLabel断言 |
| `resources/styles/main.qss` | 修改 | 导航栏 16pt + 新配色 + 选中态样式 |
| `ui/main_window.py` | 修改 | NavButton 注释说明 QSS 控制字体 |
| `dev_log/2026-07-03.md` | 新建 | 本日志 |

### 删除的旧代码
- 旧 `_fetch_plot()` 单源获取（替换为三级降级）
- 旧 `_on_plot_data()` 隐藏区块逻辑（替换为保留+占位）
- `AvatarActorWidget` → `ActorCircleWidget`（已用 QLabel 方案，本次确认无 QPainter 残留）
- 所有渲染分支中的"暂无评分"QLabel 文本

### 新增的 QSS 规则
- `QPushButton#navButton { font-size: 16pt; font-weight: 500; padding-left: 16px; min-height: 48px; }`
- `QPushButton#navButton:hover { background: #EBF8FF; color: #2B6CB0; border-left: 4px solid #3182CE; }`
- `QPushButton#navButton:checked { background: #EBF8FF; color: #2B6CB0; border-left: 4px solid #3182CE; }`

### 新增的数据源请求逻辑
- `_fetch_douban_frodo_summary()` - 豆瓣 Frodo API 获取剧情
- `_fetch_tmdb_summary()` - TMDB API 搜索+获取概述
- `_fetch_plot()` 三级降级（猫眼H5 → 豆瓣Frodo → TMDB）
- `_fetch_reviews()` 二级降级（猫眼H5 → 豆瓣Frodo）
- `_make_review_card()` 短评卡片 UI（已恢复）
