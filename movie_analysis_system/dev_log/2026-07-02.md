# 开发日志 — 2026-07-02

## 今日完成

### 重大问题修复
1. **海报显示色块问题**：根因是 `maoyan_spider.py` 中 `src.split("?")[0]` 去掉了 CDN 必需的 query 参数（`?imageView2/1/w/320/h/440`），导致 HTTP 400。修复为保留完整 URL。
2. **榜单完全一致问题**：根因是爬取的在映电影 `rating_count=0`（猫眼列表页不含此数据）。解决方案：
   - 改为**增量合并策略**（不清空已有数据，仅更新爬虫字段）
   - 保留 CSV 备份的 23 部在映电影完整数据（含 rating_count/box_office/price）
   - 5 个榜单现可明显分化（见验证日志）
3. **电影数量不足**：翻页爬取支持（offset=0,20,40...），现共 41 部在映电影（18 部实时爬取 + 23 部 CSV 备份）
4. **票价/票房缺失**：底部信息行显示 "猫眼评分 9.7 · ¥35-58 · 累计票房 970万"，无数据时显示 "¥?" 带 tooltip

### 新增功能
1. **详情页 Maoyan 风格重写**：
   - 大图海报 200×300（带阴影）+ 右侧信息区（3 区块）
   - 评分卡片：★ 金色 28pt + 评分 + 人评 + 票房 + 票价横向排列
   - 主演阵容：24×24 圆形首字头像 + 姓名
   - 剧情简介：来自 CSV 或红色 "数据未同步"
   - 特惠购票按钮：#E53E3E 红色，hover 变深
   - 同类型评分对比图（柱状图 + 颜色区分）
2. **推荐卡片点击跳转详情页**（DoubanCard → 导航信号 → QStackedWidget）
3. **Pyppeteer → Selenium 详情爬虫**（但被猫眼反爬 CAPTCHA 拦截）
4. **RANK_WEIGHTS 配置区**：所有排名参数可在 `recommender.py` 顶部调整

### 待解决
- Selenium 无头浏览器被 Maoyan CAPTCHA 拦截 (`# CRITICAL: NEED SELENIUM`)
- 爬取电影缺少 rating_count/box_office（CSV 备份的 23 部有完整数据）

## 代码变更
- `crawler/maoyan_spider.py` — 翻页爬取 + 海报 URL 修复
- `crawler/maoyan_detail_scraper.py` — Selenium 详情爬虫（新文件）
- `crawler/crawl_controller.py` — 增量合并 + CSV 补充
- `recommendation/scorer.py` — 5 个独立排名函数
- `recommendation/recommender.py` — RANK_WEIGHTS 配置 + 性价比模式
- `ui/recommendation_page.py` — 海报修复 + 票价展示 + 卡片点击
- `ui/detail_page.py` — Maoyan 风格完整重写
- `ui/main_window.py` — 推荐卡片导航信号连接
